import { Plugin } from 'rollup';
export declare type BundleOptions = {
    plugins: Plugin[];
    target: 'node' | 'browser' | 'runtime';
    format?: 'esm' | 'commonjs' | 'iife';
    treeshake?: boolean;
};
export { IO } from './types';
export declare function bundleRuleset(input: string, { target, plugins, format, treeshake }: BundleOptions): Promise<string>;
